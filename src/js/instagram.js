$('.instagram-carrousel').slick({
    prevArrow: false,
    nextArrow: false,
    centerMode: true,
    slidesToShow: 1
    // infinite: true,
    // speed: 500,
    // autoplay: true
    // autoplaySpeed: 3000
  });